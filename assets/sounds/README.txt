Notification sound for new leads:

  new-lead.wav  — included (minimal silent placeholder so playback works).

To use a custom sound: replace new-lead.wav with your own short file (<1 second),
or add new-lead.mp3 and update offscreen.js to use it. You can use a free sound
from freesound.org (e.g. "notification" or "soft chime").
